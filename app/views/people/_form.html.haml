= form_for(@person) do |f|
  - if @person.errors.any?
    %div#iderror_explanation
      %h2
        =pluralize(@person.errors.count, "error")
        prohibited this person from being saved:
      %ul
      -@person.errors.full_messages.each do |msg|
        %li= msg

  %div.actions
    = f.submit